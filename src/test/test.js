import accountControllerTest from "./account.controller.test.js";
import util from "../util/util.js";
util.Log("system", "=".repeat(50));
util.Log("system", `CONDUCTING TEST: Account Controller`);
util.Log("system", "=".repeat(50));
accountControllerTest();
